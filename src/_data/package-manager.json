{
    "$schema": "./schema.json",
    "title": "packageManager",
    "features": [
        {
            "title": "Supports big 3 🐇 runtimes (Node.JS, deno, bun)",
            "npm": {
                "warning": true,
                "note": "Only Node.JS and Deno"
            }
        }
    ]
    
}